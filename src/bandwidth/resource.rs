pub struct BandwidthStatistic {
    pub name: String,
    pub address: String,
    pub maximum_transmission_unit: String,
    pub upload: String,
    pub upload_rate: u64,
    pub download: String,
    pub download_rate: u64,
    pub total: String
}
